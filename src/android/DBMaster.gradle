allprojects {
  repositories {
    mavenCentral()
    flatDir {
        dirs 'aar'
    }
  }
}

dependencies {
  compile(name:'DBMaster', ext:'aar')
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}